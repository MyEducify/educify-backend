#!/bin/sh
. "$(dirname "$0")/_/husky.sh"


echo "🧼 Auto-formatting uncommitted C# files..."

FILES=$(git diff --cached --name-only --diff-filter=ACM | grep '\.cs$')
if [ -n "$FILES" ]; then
  dotnet format --include $FILES
  git add $FILES
fi
echo "✅ All files properly formatted."